<h1>Bonjour, {{ app.user.fullName }}</h1>

<p>Vous avez demandé la réinitialisation de votre mot de passe. </p>

<a href="{{ url('reset_password', {token: resetToken.token}) }}">{{ url('reset_password', {token: resetToken.token}) }}</a>

<p>Le lien ne sera plus valide d'ici {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cordialement</p>
